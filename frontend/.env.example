# Optional. Defaults: 5000 (Linux/Windows), 5001 (macOS) so Vite matches python app.py.
# VITE_BACKEND_PORT=5001
#
# Production build: full origin of Flask (no trailing slash). Leave unset for local dev `/api` proxy.
# VITE_API_ORIGIN=https://your-api.onrender.com
